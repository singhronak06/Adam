extern void sort(int size, int x[]);

extern float mean(int size, int x[]);

extern float median(int size, int x[]); 
 
extern float mode(int size, int x[]);

extern int max(int size, int x[]);

extern int min(int size, int x[]);
